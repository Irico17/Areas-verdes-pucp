# data/raw — baseline recuperado

Copia del monolito Leaflet y snapshots públicos desde `/workspace/mapa-web-6-recovery/`.

- `*.geojson`, `jefe_de_grupo.json` — capas estáticas
- `legacy-app/` — `index.html`, `style.css`, `script.js` (referencia, no mantener)
- `sheets/` — CSV publicados (reservas = `reservas_jardines.FAILED.txt`, 401)
- `gviz/`, `appscript/` — snapshots auxiliares
- `drive_fotos/` — `_index.json` + JPGs de bebederos (~8.8 MB)

No editar in-place; transformar hacia `data/v1/`.
